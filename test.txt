
╭─── Claude Code v2.0.76 ──────────────────────────────────────────────────────╮
│                                   │ Tips for getting started                 │
│           Welcome back!           │ Run /init to create a CLAUDE.md file wi… │
│                                   │ ──────────────────────────────────────── │
│            * ▐▛███▜▌ *            │ Recent activity                          │
│           * ▝▜█████▛▘ *           │ No recent activity                       │
│            *  ▘▘ ▝▝  *            │                                          │
│                                   │                                          │
│    glm-4.7 · API Usage Billing    │                                          │
│   D:\momo\code\git\astron-agent   │                                          │
╰──────────────────────────────────────────────────────────────────────────────╯


 ‼Auth conflict: Both a token (ANTHROPIC_AUTH_TOKEN) and an API key 
  (ANTHROPIC_API_KEY) are set. This may lead to unexpected behavior.
    • Trying to use ANTHROPIC_AUTH_TOKEN? Unset the ANTHROPIC_API_KEY 
    environment variable, or claude /logout then say "No" to the API key 
    approval before login.
    • Trying to use ANTHROPIC_API_KEY? Unset the ANTHROPIC_AUTH_TOKEN 
    environment variable.
  /model to try Opus 4.5

> /resume 
  ⎿  Resume cancelled

> @console\backend\hub\src\main\java\com\iflytek\astron\console\hub\controll
er\wechat\WechatBotCallBackController.java 检查handleCallback方法，当接受消
息接收与处理（POST请求）时，是否符合企微智能机器人的开发要求，    "msgtype":
 "stream",
    "stream": {
        "id": "STREAMID"
    }
}
文本消息
{
    "msgid": "CAIQ16HMjQYY\/NGagIOAgAMgq4KM0AI=",
    "aibotid": "AIBOTID",
    "chatid": "CHATID",
    "chattype": "group",
    "from": {
        "userid": "USERID"
    },
    "response_url": "RESPONSEURL",
    "msgtype": "text",
    "text": {
        "content": "@RobotA hello robot"
    },
    "quote": {
        "msgtype": "text",
        "text": {
            "content": "这是今日的测试情况"
        }
    }
}

被动回复消息
流式消息回复
{
    "msgtype": "stream",
    "stream": {
        "id": "STREAMID",
        "finish": false,
        "content": "**广州**今日天气：29度，大部分多云，降雨概率：60%",
        "msg_item": [
            {
                "msgtype": "image",
                "image": {
                    "base64": "BASE64",
                    "md5": "MD5"
                }
            }
        ],
        "feedback": {
            "id": "FEEDBACKID"
        }
    }
} 

● 根据企业微信智能机器人的开发要求，我来检查 handleCallback 方法的实现：

  当前实现分析

  符合要求的地方：

  1. 统一的回调端点：同时处理 GET（URL验证）和 POST（消息接收）请求 ✓
  2. 参数接收完整：接收了所有必要参数（msg_signature、timestamp、nonce、echostr、postData）✓
  3. URL验证逻辑：GET 请求时正确处理 echostr ✓
  4. 签名验证准备：将签名相关参数传递给服务层处理 ✓

  潜在问题：

  1. 响应格式处理不明确

  从你提供的企业微信机器人响应格式来看，流式回复需要返回特定的 JSON 格式：

  {
      "msgtype": "stream",
      "stream": {
          "id": "STREAMID",
          "finish": false,
          "content": "消息内容",
          "msg_item": [...]
      }
  }

  当前 handleCallback 方法直接返回 wechatBotService.handleMessage() 的结果，但返回类型是 String，这意味着：
  - 如果返回的是 JSON 字符串，需要确保 Content-Type 头正确设置为 application/json
  - 需要确认 handleMessage 方法返回的格式是否符合企业微信的要求

  2. 没有设置响应头

  @RequestMapping(value = "/callback/{botKey}", method = {RequestMethod.GET, RequestMethod.POST})
  public String handleCallback(...)

  建议添加 produces = "application/json" 或使用 @ResponseBody 配合 ResponseEntity 来确保正确的响应格式。

  建议修改：

  @RequestMapping(
      value = "/callback/{botKey}",
      method = {RequestMethod.GET, RequestMethod.POST},
      produces = MediaType.APPLICATION_JSON_VALUE
  )
  public ResponseEntity<String> handleCallback(
          @Parameter(description = "机器人key") @PathVariable String botKey,
          @RequestParam(value = "msg_signature", required = false) String msgSignature,
          @RequestParam(value = "timestamp", required = false) String timestamp,
          @RequestParam(value = "nonce", required = false) String nonce,
          @RequestParam(value = "echostr", required = false) String echostr,
          @RequestBody(required = false) String postData) {
      // ...
  }

  需要我进一步检查 WechatBotService 的实现来确认 handleMessage 方法的响应格式是否正确吗？

> @aibot_demo_python3\demo_server.py 
这个是官方的python案例，请检查目前的需求流程是否有问题：被动回复流程
1.解密
2.解析数据
3.根据数据类型获取回复
4.调用原来的聊天接口
5.封装流式数据类型返回
    企微流式数据最多等待6分钟
    收到stream的类型，带有id的请求，发送完全的回复
6.加密返回 

● 根据官方Python示例代码，我来检查你设计的被动回复流程：

  流程对比分析

  你的流程 vs 官方实现

  | 步骤                    | 你的流程 | 官方Python实现                    | 状态 |
  |-------------------------|----------|-----------------------------------|------|
  | 1. 解密                 | ✓        | 第245-253行 DecryptMsg            | ✓    |
  | 2. 解析数据             | ✓        | 第255行 json.loads(msg)           | ✓    |
  | 3. 根据数据类型获取回复 | ✓        | 第261-311行按msgtype分支          | ✓    |
  | 4. 调用原来的聊天接口   | ✓        | 第266-268行 llm.invoke/get_answer | ✓    |
  | 5. 封装流式数据类型返回 | ✓        | 第271行 MakeTextStream            | ✓    |
  | 6. 加密返回             | ✓        | 第272行 EncryptMessage            | ✓    |

  ---
  发现的问题

  1. receiveid 参数的处理

  # 官方示例第211行和第242行
  receiveid = ''  # 智能机器人的 receiveid 是空字符串

  问题：当前Java代码使用 botKey 作为 receiveid，但智能机器人应该传空字符串。

  2. 响应的 media_type

  # 官方示例第224、273、283行
  return Response(content=resp, media_type="text/plain")

  问题：即使返回的是加密后的JSON数据，Content-Type 必须是 text/plain，不是 application/json。

  3. 流式消息轮询机制

  官方示例的关键点：
  # 第262-273行：首次收到text消息，返回第一个流式响应
  elif (msgtype == 'stream'):  # 第274-283行
      # 企业微信会继续用stream类型轮询，直到 finish=true
      stream_id = data['stream']['id']
      llm = LLMDemo()
      answer = llm.get_answer(stream_id)
      finish = llm.is_task_finish(stream_id)

  问题：
  - 企业微信会持续轮询，直到 finish=true
  - Java实现需要保存 stream_id 和对应的任务状态
  - 你当前的 handleMessage 是无状态的，无法支持轮询

  ---
  需要补充的要点

  1. 创建响应工具类

  参考Python的 MakeTextStream 和 MakeImageStream，Java需要：

  // 文本流式响应
  {
      "msgtype": "stream",
      "stream": {
          "id": "STREAMID",
          "finish": false,
          "content": "消息内容"
      }
  }

  // 图片流式响应
  {
      "msgtype": "stream",
      "stream": {
          "id": "STREAMID",
          "finish": true,
          "msg_item": [{
              "msgtype": "image",
              "image": {
                  "base64": "BASE64",
                  "md5": "MD5"
              }
          }]
      }
  }

  2. 加密消息

  # EncryptMessage(receiveid, nonce, timestamp, stream)
  # receiveid = '' (智能机器人)
  # nonce, timestamp: 从请求参数获取
  # stream: JSON格式的流式响应

  Java需要实现对应的加密逻辑（WXBizJsonMsgCrypt）。

  ---
  总结

  | 检查项         | 结果                |
  |----------------|---------------------|
  | 流程正确性     | ✓ 基本正确          |
  | receiveid 处理 | ✗ 需要改为空字符串  |
  | Content-Type   | ✗ 必须是 text/plain |
  | 流式轮询状态   | ✗ 需要状态管理支持  |

  需要我检查当前Java的 WechatBotService 实现来具体指出需要修改的地方吗？

> 对于receiveid的处理：@console\backend\hub\src\main\java\com\iflytek\astron
\console\hub\service\wechat\impl\WechatBotServiceImpl.java 中有// 
使用空字符串作为receiveId，符合智能机器人场景
        WXBizMsgCrypt pc = new WXBizMsgCrypt(config.getToken(), 
config.getEncodingAesKey(), ""); 

● Read(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\service\
      wechat\impl\WechatBotServiceImpl.java)
  ⎿  Read 206 lines

● 确认，receiveid 处理正确（第152行和第190行）。现在让我检查 processMessageSync 方法的实现：

● Read(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\service\
      wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Read 1048 lines

● 根据代码分析，我发现了一个关键问题：

  问题：stream 字段名不一致

  官方Python示例（第166行）

  streamRefreshContent.setStreamId(streamRefreshJson.getString("streamId"))

  从官方Python示例和企微文档，流式消息的JSON格式是：
  {
      "msgtype": "stream",
      "stream": {
          "id": "STREAMID"
      }
  }

  字段名是 id，不是 streamId！

  当前Java实现（第164-171行）

  case "stream":
      JSONObject streamRefreshJson = json.getJSONObject("stream");
      if (streamRefreshJson != null) {
          WechatRobotMessageDto.StreamRefreshContent streamRefreshContent = new WechatRobotMessageDto.StreamRefreshContent();
          streamRefreshContent.setStreamId(streamRefreshJson.getString("streamId"));  // ❌ 错误！
          messageDto.setStreamRefresh(streamRefreshContent);
      }
      break;

  这里使用了错误的字段名 "streamId"，应该改为 "id"。

  ---
  其他检查项

  | 检查项               | 状态 | 说明                               |
  |----------------------|------|------------------------------------|
  | receiveid 为空字符串 | ✓    | 第190行、第1005行正确              |
  | 解密消息             | ✓    | 第193行 decryptMsg                 |
  | 解析数据             | ✓    | 第197行 parseMessage               |
  | 流式ID生成           | ✓    | 第381行 UUID                       |
  | 异步流处理           | ✓    | 第449行 CompletableFuture.runAsync |
  | 流式消息存储         | ✓    | 第512行 storeStreamMessage         |
  | 流式刷新处理         | ✓    | 第567行 processStreamRefresh       |
  | 加密返回             | ✓    | 第1006行 encryptMsg                |
  | 响应格式             | ✓    | JSON格式正确                       |

  ---
  需要修复的代码

● Read(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\dto\wech
      at\WechatRobotMessageDto.java)
  ⎿  Read 212 lines

● 确认发现了问题！需要修改 WechatRobotMessageServiceImpl.java 第168行：

  需要修复的代码

  第164-171行 - 错误的字段名 "streamId"：

  case "stream":
      JSONObject streamRefreshJson = json.getJSONObject("stream");
      if (streamRefreshJson != null) {
          WechatRobotMessageDto.StreamRefreshContent streamRefreshContent = new WechatRobotMessageDto.StreamRefreshContent();
          streamRefreshContent.setStreamId(streamRefreshJson.getString("streamId"));  // ❌ 错误！
          messageDto.setStreamRefresh(streamRefreshContent);
      }
      break;

  应改为：

  case "stream":
      JSONObject streamRefreshJson = json.getJSONObject("stream");
      if (streamRefreshJson != null) {
          WechatRobotMessageDto.StreamRefreshContent streamRefreshContent = new WechatRobotMessageDto.StreamRefreshContent();
          streamRefreshContent.setStreamId(streamRefreshJson.getString("id"));  // ✓ 正确
          messageDto.setStreamRefresh(streamRefreshContent);
      }
      break;

  ---
  修复原因

  根据企业微信官方文档和Python示例：
  {
      "msgtype": "stream",
      "stream": {
          "id": "STREAMID"  // ← 字段名是 "id"
      }
  }

  不是 "streamId"，而是 "id"。这会导致收到流式刷新请求时无法正确解析 streamId，从而使 processStreamRefreshMessage 获取到 null。

  ---
  总结

  除了这个字段名问题，你的被动回复流程设计基本正确，符合企业微信智能机器人的开发要求。

  需要我帮你修改这个字段名吗？

> 我已修改，请帮我修复流式轮询状态的问题 

● 我来分析并修复流式轮询状态的问题。让我先创建待办列表来跟踪修复进度：

● 问题分析

  当前流式轮询状态存在以下问题：

  | 问题           | 位置                       | 说明                                                |
  |----------------|----------------------------|-----------------------------------------------------|
  | 初始状态未设置 | processTextMessageSync     | 创建 streamId 后没有初始化 streamStatusMap 为 false |
  | 空指针风险     | processStreamRefresh:576   | streamStatusMap.get(streamId) 可能为 null           |
  | 状态未初始化   | startAsyncStreamingProcess | 异步任务开始时没有设置初始状态                      |

  ---
  开始修复

● Update(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Error editing file

● Read(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\service\
      wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Read 1048 lines

● 现在开始修复流式轮询状态问题。首先是修复初始状态未设置的问题：

● Update(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Added 9 lines, removed 5 lines                                        
      373              } else if (messageDto.getQuote() != null) {       
      374                  content = messageDto.getQuote().getContent(); 
      375              }                                                 
      376 -                                                              
      377 -            log.info("Processing text message synchronously   
          -from user {}: {}",                                            
      376 +                                                              
      377 +            log.info("Processing text message synchronously   
          +from user {}: {}",                                            
      378                      messageDto.getFrom().getUserId(),         
           content);                                                     
      379                                                                
      380              // 生成流式ID                                        


      381              String streamId =                                 
           UUID.randomUUID().toString().substring(0, 10);                
      382 -                                                              
      382 +                                                              
      383 +            // 初始化流式处理状态为                              
            

          +false（处理中），确保后续轮询能正确识别                          
                   

      384 +            // 必须在启动异步任务前初始化状态，避免竞态条件      
                        

      385 +            streamStatusMap.computeIfAbsent(streamId, k ->    
          +new AtomicBoolean(false));                                    
      386 +                                                              
      387              // 启动异步流式处理任务                              


      388              startAsyncStreamingProcess(content, messageDto,   
           config, timestamp, nonce, streamId);                          
      389 -                                                              
      389 +                                                              
      390              // 立即返回流式开始消息（符合企微要求）              


      391              return createStreamReply(streamId, "🔍            
           正在为您处理请求...", false, timestamp, nonce, config);          


      392 -                                                              
      392 +                                                              
      393          } catch (Exception e) {                               
      394              log.error("Failed to process text message         
           synchronously", e);                                           
      395              return                                            
           createTextStreamReply("处理消息时发生错误：" +                   


           e.getMessage(), timestamp, nonce, config);                    

● 现在修复流式刷新时的空指针问题：

● Update(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Added 12 lines, removed 6 lines                                       
      571      public String processStreamRefresh(String streamId,       
           String timestamp, String nonce, WechatBotConfig config) {     
      572          try {                                                 
      573              log.debug("Processing stream refresh for          
           streamId: {}", streamId);                                     
      574 -                                                              
      574 +                                                              
      575 +            // 检查 streamId 是否存在                            
        

      576 +            if (!streamStatusMap.containsKey(streamId)) {     
      577 +                log.warn("Unknown streamId in refresh         
          +request: {}", streamId);                                      
      578 +                return createStreamReply(streamId, "❌         
          +请求已过期或不存在", true, timestamp, nonce, config);            
           

      579 +            }                                                 
      580 +                                                              
      581              // 获取该streamId的消息队列                          


      582              Queue<String> messageQueue =                      
           streamMessageStorage.get(streamId);                           
      583 -                                                              
      583 +                                                              
      584              if (messageQueue == null ||                       
           messageQueue.isEmpty()) {                                     
      585                  // 检查是否已完成处理                            


      586                  AtomicBoolean isCompleted =                   
           streamStatusMap.get(streamId);                                
     ...
      586                      return createStreamReply(streamId, "⏳     
           仍在处理中，请稍候...", false, timestamp, nonce, config);        


      587                  }                                             
      588              }                                                 
      589 -                                                              
      589 +                                                              
      590              // 从队列中取出下一个消息                            


      591              String nextMessage = messageQueue.poll();         
      592              if (nextMessage != null) {                        
      593 -                log.debug("Retrieved stream message for       
          -streamId: {}, remaining queue size: {}",                      
      593 +                log.debug("Retrieved stream message for       
          +streamId: {}, remaining queue size: {}",                      
      594                           streamId, messageQueue.size());      
      595                  return nextMessage;                           
      596              }                                                 
      597 -                                                              
      597 +                                                              
      598              // 队列为空但处理未完成                              


      599              return createStreamReply(streamId, "⏳             
           继续处理中...", false, timestamp, nonce, config);                


      600 -                                                              
      600 +                                                              
      601          } catch (Exception e) {                               
      602              log.error("Failed to process stream refresh for   
           streamId: {}", streamId, e);                                  
      603              return createStreamReply(streamId, "❌             
           刷新处理失败", true, timestamp, nonce, config);                  



● 现在优化完成状态的判断逻辑，确保异步任务开始时正确初始化状态：

● Read(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\service\
      wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Read 70 lines

● Read(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\service\
      wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Read 100 lines

● 优化 CapturingSseEmitter 中的状态管理逻辑，确保使用已存在的状态而不是重新创建：

● Update(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Added 25 lines, removed 11 lines                                      
      617          private final String nonce;                           
      618          private final WechatBotConfig config;                 
      619          private final AtomicInteger messageCount;             
      620 -                                                              
      621 -        public CapturingSseEmitter(long timeout, String       
          -streamId, String timestamp, String nonce,                     
      622 -                                 WechatBotConfig config,      
          -List<String> capturedMessages,                                
      620 +                                                              
      621 +        public CapturingSseEmitter(long timeout, String       
          +streamId, String timestamp, String nonce,                     
      622 +                                 WechatBotConfig config,      
          +List<String> capturedMessages,                                
      623                                   StringBuilder                
           completeContent, AtomicInteger messageCount) {                
      624              super(timeout);                                   
      625              this.streamId = streamId;                         
     ...
      629              this.capturedMessages = capturedMessages;         
      630              this.completeContent = completeContent;           
      631              this.messageCount = messageCount;                 
      632 -                                                              
      632 +                                                              
      633              // 设置完成监听器                                    


      634              this.onCompletion(() -> {                         
      635                  log.debug("CapturingSseEmitter completed for  
           streamId: {}", streamId);                                     
      636                  // 发送最终完成消息并存储                        


      637 -                String finalMessage =                         
          -createStreamReply(this.streamId,                              
          -this.completeContent.toString(), true,                        
      637 +                String finalMessage =                         
          +createStreamReply(this.streamId,                              
          +this.completeContent.toString(), true,                        
      638                                                                
           this.timestamp, this.nonce, this.config);                     
      639                  this.capturedMessages.add(finalMessage);      
      640                  storeStreamMessage(this.streamId,             
           finalMessage);                                                
      641 -                // 标记处理完成                                  
        

      642 -                                                              
          -streamStatusMap.computeIfAbsent(this.streamId, k -> new       
          -AtomicBoolean(false)).set(true);                              
      641 +                // 标记处理完成 - 使用已存在的状态，不重新创建   
                      

      642 +                AtomicBoolean status =                        
          +streamStatusMap.get(this.streamId);                           
      643 +                if (status != null) {                         
      644 +                    status.set(true);                         
      645 +                    log.debug("Stream processing completed    
          +and marked for streamId: {}", streamId);                      
      646 +                } else {                                      
      647 +                    log.warn("Stream status not found for     
          +streamId: {}, creating new status", streamId);                
      648 +                    streamStatusMap.put(this.streamId, new    
          +AtomicBoolean(true));                                         
      649 +                }                                             
      650              });                                               
      651 -                                                              
      651 +                                                              
      652              // 设置错误监听器                                    


      653              this.onError(throwable -> {                       
      654                  log.error("CapturingSseEmitter error for      
           streamId: {}", streamId, throwable);                          
      655 -                String errorMessage =                         
          -createStreamReply(this.streamId, "处理过程中出现错误", true,     
           

      655 +                String errorMessage =                         
          +createStreamReply(this.streamId, "处理过程中出现错误", true,     
           

      656                                                                
           this.timestamp, this.nonce, this.config);                     
      657                  storeStreamMessage(this.streamId,             
           errorMessage);                                                
      658 -                // 标记处理完成（出错也算完成）                  
                

      659 -                                                              
          -streamStatusMap.computeIfAbsent(this.streamId, k -> new       
          -AtomicBoolean(false)).set(true);                              
      658 +                // 标记处理完成（出错也算完成）- 
     使用已存在的状态，不重新创建             
      659 +                AtomicBoolean status =                        
          +streamStatusMap.get(this.streamId);                           
      660 +                if (status != null) {                         
      661 +                    status.set(true);                         
      662 +                    log.debug("Stream processing marked as    
          +failed for streamId: {}", streamId);                          
      663 +                } else {                                      
      664 +                    log.warn("Stream status not found for     
          +streamId: {}, creating new status", streamId);                
      665 +                    streamStatusMap.put(this.streamId, new    
          +AtomicBoolean(true));                                         
      666 +                }                                             
      667              });                                               
      668          }                                                     
      669                                                                

● 同样修复 startAsyncStreamingProcess 中的状态管理：

● Update(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Added 21 lines, removed 14 lines                                      
      447       * 启动异步流式处理任务                                      


      448       * 符合企业微信流式消息刷新机制                              


      449       */                                                       
      450 -    private void startAsyncStreamingProcess(String content,   
          -WechatRobotMessageDto messageDto,                             
      450 +    private void startAsyncStreamingProcess(String content,   
          +WechatRobotMessageDto messageDto,                             
      451                                            WechatBotConfig     
           config, String timestamp, String nonce, String streamId) {    
      452          // 使用线程池异步处理，避免阻塞主线程                    


      453          CompletableFuture.runAsync(() -> {                    
      454              try {                                             
      455 -                log.info("Starting async streaming process for
          - streamId: {}, userId: {}",                                   
      455 +                log.info("Starting async streaming process for
          + streamId: {}, userId: {}",                                   
      456                          streamId,                             
           messageDto.getFrom().getUserId());                            
      457 -                                                              
      457 +                                                              
      458                  // 执行清理操作                                  


      459                  cleanupExpiredStreams();                      
      460 -                                                              
      460 +                                                              
      461                  // 构造聊天请求                                  


      462                  ChatBotReqDto chatBotReqDto = new             
           ChatBotReqDto();                                              
      463                  chatBotReqDto.setAsk(content);                
     ...
      465                  chatBotReqDto.setBotId(Integer.valueOf(config 
           .getAgentIdRef()));                                           
      466                  chatBotReqDto.setChatId(1L);                  
      467                  chatBotReqDto.setEdit(false);                 
      468 -                                                              
      468 +                                                              
      469                  // 创建用于捕获流式消息的Emitter                 


      470                  List<String> streamMessages = new             
           ArrayList<>();                                                
      471                  StringBuilder completeReply = new             
           StringBuilder();                                              
      472                  AtomicInteger messageCounter = new            
           AtomicInteger(0);                                             
      473 -                                                              
      473 +                                                              
      474                  CapturingSseEmitter capturingEmitter = new    
           CapturingSseEmitter(                                          
      475                      360000L, // 6分钟超时                        


      476                      streamId,                                 
     ...
      481                      completeReply,                            
      482                      messageCounter                            
      483                  );                                            
      484 -                                                              
      484 +                                                              
      485                  String sseId = UUID.randomUUID().toString();  
      486 -                                                              
      486 +                                                              
      487                  // 调用真实的bot聊天服务                         


      488                  botChatService.chatMessageBot(chatBotReqDto,  
           capturingEmitter, sseId, null, null);                         
      489 -                                                              
      490 -                log.info("Async streaming process completed   
          -for streamId: {}, total messages: {}",                        
      489 +                                                              
      490 +                log.info("Async streaming process completed   
          +for streamId: {}, total messages: {}",                        
      491                          streamId, streamMessages.size());     
      492 -                                                              
      492 +                                                              
      493                  // 定期清理过期数据                              


      494                  if (Math.random() < 0.1) { // 10%概率执行清理    


      495                      cleanupExpiredStreams();                  
      496                  }                                             
      497 -                                                              
      497 +                                                              
      498              } catch (Exception e) {                           
      499                  log.error("Async streaming process failed for 
            streamId: {}", streamId, e);                                 
      500                  // 发送错误消息                                  


      501                  try {                                         
      502                      String errorMessage =                     
           createStreamReply(streamId, "❌ 处理过程中出现错误", true,       


           timestamp, nonce, config);                                    
      503                      storeStreamMessage(streamId,              
           errorMessage);                                                
      504 -                    // 标记处理完成（出错也算完成）              
                

      505 -                    streamStatusMap.computeIfAbsent(streamId, 
          - k -> new AtomicBoolean(false)).set(true);                    
      504 +                    // 标记处理完成（出错也算完成）- 
     使用已存在的状态，不重新创建         
      505 +                    AtomicBoolean status =                    
          +streamStatusMap.get(streamId);                                
      506 +                    if (status != null) {                     
      507 +                        status.set(true);                     
      508 +                        log.debug("Stream processing marked   
          +as failed in catch block for streamId: {}", streamId);        
      509 +                    } else {                                  
      510 +                        log.warn("Stream status not found for 
          + streamId: {}, creating new status", streamId);               
      511 +                        streamStatusMap.put(streamId, new     
          +AtomicBoolean(true));                                         
      512 +                    }                                         
      513                  } catch (Exception ex) {                      
      514                      log.error("Failed to send error message   
           for streamId: {}", streamId, ex);                             
      515                  }                                             

● 流式轮询状态问题已修复完成。以下是修复的总结：

  修复内容

  1. 修复初始状态未设置问题 (第383-385行)

  // 初始化流式处理状态为 false（处理中），确保后续轮询能正确识别
  // 必须在启动异步任务前初始化状态，避免竞态条件
  streamStatusMap.computeIfAbsent(streamId, k -> new AtomicBoolean(false));

  2. 修复流式刷新空指针问题 (第575-579行)

  // 检查 streamId 是否存在
  if (!streamStatusMap.containsKey(streamId)) {
      log.warn("Unknown streamId in refresh request: {}", streamId);
      return createStreamReply(streamId, "❌ 请求已过期或不存在", true, timestamp, nonce, config);
  }

  3. 优化完成状态判断逻辑

  CapturingSseEmitter.onCompletion (第642-649行)

  // 标记处理完成 - 使用已存在的状态，不重新创建
  AtomicBoolean status = streamStatusMap.get(this.streamId);
  if (status != null) {
      status.set(true);
      log.debug("Stream processing completed and marked for streamId: {}", streamId);
  } else {
      log.warn("Stream status not found for streamId: {}, creating new status", streamId);
      streamStatusMap.put(this.streamId, new AtomicBoolean(true));
  }

  CapturingSseEmitter.onError (第659-666行)

  // 标记处理完成（出错也算完成）- 使用已存在的状态，不重新创建
  AtomicBoolean status = streamStatusMap.get(this.streamId);
  if (status != null) {
      status.set(true);
      log.debug("Stream processing marked as failed for streamId: {}", streamId);
  } else {
      log.warn("Stream status not found for streamId: {}, creating new status", streamId);
      streamStatusMap.put(this.streamId, new AtomicBoolean(true));
  }

  startAsyncStreamingProcess catch块 (第505-512行)

  // 标记处理完成（出错也算完成）- 使用已存在的状态，不重新创建
  AtomicBoolean status = streamStatusMap.get(streamId);
  if (status != null) {
      status.set(true);
      log.debug("Stream processing marked as failed in catch block for streamId: {}", streamId);
  } else {
      log.warn("Stream status not found for streamId: {}, creating new status", streamId);
      streamStatusMap.put(streamId, new AtomicBoolean(true));
  }

  ---
  修复效果

  | 问题       | 修复前                                | 修复后                       |
  |------------|---------------------------------------|------------------------------|
  | 初始状态   | 未初始化，可能导致轮询失败            | 启动前初始化为 false         |
  | 空指针风险 | streamStatusMap.get() 可能为 null     | 添加存在性检查               |
  | 状态不一致 | 使用 computeIfAbsent 可能创建重复状态 | 使用已存在状态，避免重复创建 |

  现在流式轮询状态管理已经修复，可以正确处理企业微信的流式消息刷新机制。

> 这段处理有返回大模型完整的回复吗？/**
     * 处理流式消息刷新请求
     * 企业微信会在未收到结束前持续推送刷新请求
     */
    public String processStreamRefresh(String streamId, String timestamp, 
String nonce, WechatBotConfig config) {
        try {
            log.debug("Processing stream refresh for streamId: {}", 
streamId);

            // 检查 streamId 是否存在
            if (!streamStatusMap.containsKey(streamId)) {
                log.warn("Unknown streamId in refresh request: {}", 
streamId);
                return createStreamReply(streamId, "❌ 请求已过期或不存在", 
true, timestamp, nonce, config);
            }

            // 获取该streamId的消息队列
            Queue<String> messageQueue = streamMessageStorage.get(streamId);

            if (messageQueue == null || messageQueue.isEmpty()) {
                // 检查是否已完成处理
                AtomicBoolean isCompleted = streamStatusMap.get(streamId);
                if (isCompleted != null && isCompleted.get()) {
                    // 已完成，返回结束消息
                    return createStreamReply(streamId, "✅ 处理已完成", 
true, timestamp, nonce, config);
                } else {
                    // 还在处理中，返回等待消息
                    return createStreamReply(streamId, "⏳ 
仍在处理中，请稍候...", false, timestamp, nonce, config);
                }
            }

            // 从队列中取出下一个消息
            String nextMessage = messageQueue.poll();
            if (nextMessage != null) {
                log.debug("Retrieved stream message for streamId: {}, 
remaining queue size: {}",
                         streamId, messageQueue.size());
                return nextMessage;
            }

            // 队列为空但处理未完成
            return createStreamReply(streamId, "⏳ 继续处理中...", false, 
timestamp, nonce, config);

        } catch (Exception e) {
            log.error("Failed to process stream refresh for streamId: {}", 
streamId, e 

● 问题分析

  这段代码没有正确返回大模型的完整回复！

  当处理完成后（isCompleted.get() == true），代码返回的是：
  return createStreamReply(streamId, "✅ 处理已完成", true, timestamp, nonce, config);

  而不是大模型的完整回复内容。用户只能看到 "✅ 处理已完成"，看不到大模型的回答。

  ---
  问题原因

  查看 CapturingSseEmitter.onCompletion 的逻辑，最终消息已经存储到队列中了：

  // 发送最终完成消息并存储
  String finalMessage = createStreamReply(this.streamId, this.completeContent.toString(), true, ...);
  storeStreamMessage(this.streamId, finalMessage);  // 已存储

  但 processStreamRefresh 在队列为空时，直接返回 "✅ 处理已完成"，没有检查队列中是否还有消息（包括最终消息）。

  ---
  修复方案

  需要修改逻辑：即使队列为空，也要在返回完成消息前再检查一次队列

● Read(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\service\
      wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Read 50 lines

● Update(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Added 26 lines, removed 18 lines                                      
      588              // 获取该streamId的消息队列                          


      589              Queue<String> messageQueue =                      
           streamMessageStorage.get(streamId);                           
      590                                                                
      591 -            if (messageQueue == null ||                       
          -messageQueue.isEmpty()) {                                     
      592 -                // 检查是否已完成处理                            
           

      593 -                AtomicBoolean isCompleted =                   
          -streamStatusMap.get(streamId);                                
      594 -                if (isCompleted != null && isCompleted.get()) 
          - {                                                            
      595 -                    // 已完成，返回结束消息                      
            

      596 -                    return createStreamReply(streamId, "✅     
          -处理已完成", true, timestamp, nonce, config);                    
       

      597 -                } else {                                      
      598 -                    // 还在处理中，返回等待消息                  
              

      599 -                    return createStreamReply(streamId, "⏳     
          -仍在处理中，请稍候...", false, timestamp, nonce, config);        
           

      591 +            // 优先从队列中获取消息                              
            

      592 +            if (messageQueue != null &&                       
          +!messageQueue.isEmpty()) {                                    
      593 +                String nextMessage = messageQueue.poll();     
      594 +                if (nextMessage != null) {                    
      595 +                    log.debug("Retrieved stream message for   
          +streamId: {}, remaining queue size: {}",                      
      596 +                             streamId, messageQueue.size());  
      597 +                    return nextMessage;                       
      598                  }                                             
      599              }                                                 
      600                                                                
      601 -            // 从队列中取出下一个消息                            
             

      602 -            String nextMessage = messageQueue.poll();         
      603 -            if (nextMessage != null) {                        
      604 -                log.debug("Retrieved stream message for       
          -streamId: {}, remaining queue size: {}",                      
      605 -                         streamId, messageQueue.size());      
      606 -                return nextMessage;                           
      601 +            // 队列为空时，检查是否已完成处理                    
                 

      602 +            AtomicBoolean isCompleted =                       
          +streamStatusMap.get(streamId);                                
      603 +            if (isCompleted != null && isCompleted.get()) {   
      604 +                //                                            
          +已完成，检查是否还有最后一次消息（完成消息）                     
                        

      605 +                // 注意：onCompletion中会将最终消息存储到队列    
                 

          +，但由于竞态条件可能还没被poll到                                 
                

      606 +                // 再次检查队列，确保获取到最终消息              
                  

      607 +                if (messageQueue != null &&                   
          +!messageQueue.isEmpty()) {                                    
      608 +                    String finalMessage =                     
          +messageQueue.poll();                                          
      609 +                    if (finalMessage != null) {               
      610 +                        log.debug("Retrieved final stream     
          +message for streamId: {}", streamId);                         
      611 +                        return finalMessage;                  
      612 +                    }                                         
      613 +                }                                             
      614 +                // 真正没有消息了，返回结束标记                  
                

      615 +                log.debug("Stream processing truly completed  
          +for streamId: {}", streamId);                                 
      616 +                return createStreamReply(streamId, "", true,  
          +timestamp, nonce, config);                                    
      617 +            } else {                                          
      618 +                // 还在处理中，返回等待消息                      
              

      619 +                return createStreamReply(streamId, "⏳         
          +仍在处理中，请稍候...", false, timestamp, nonce, config);        
           

      620              }                                                 
      621                                                                
      622 -            // 队列为空但处理未完成                              
            

      623 -            return createStreamReply(streamId, "⏳             
          -继续处理中...", false, timestamp, nonce, config);                
       

      624 -                                                              
      622          } catch (Exception e) {                               
      623              log.error("Failed to process stream refresh for   
           streamId: {}", streamId, e);                                  
      624              return createStreamReply(streamId, "❌             
           刷新处理失败", true, timestamp, nonce, config);                  



● 等等，我发现了一个更深层的问题！

  让我检查 CapturingSseEmitter.send 方法中如何存储中间消息：

● Read(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\service\
      wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Read 40 lines

● 我发现还有一个关键问题：在 CapturingSseEmitter.send 中，每次捕获的中间消息都存储到队列中，但企微的刷新机制可能不会正确获取这些中间消息。

  让我重新分析企微的官方Python示例流程：

  # 首次text消息
  llm = LLMDemo()
  stream_id = llm.invoke(content)
  answer = llm.get_answer(stream_id)  # 获取部分回复
  finish = llm.is_task_finish(stream_id)  # 检查是否完成

  stream = MakeTextStream(stream_id, answer, finish)  # finish=false
  resp = EncryptMessage(receiveid, nonce, timestamp, stream)
  return resp

  # 后续stream消息
  stream_id = data['stream']['id']
  llm = LLMDemo()
  answer = llm.get_answer(stream_id)  # 获取更多回复
  finish = llm.is_task_finish(stream_id)  # 检查是否完成

  stream = MakeTextStream(stream_id, answer, finish)  # 最终 finish=true
  resp = EncryptMessage(receiveid, nonce, timestamp, stream)
  return resp

  关键区别：Python每次刷新都调用 get_answer() 获取增量内容，而我们的Java实现是预先存储所有消息到队列。

  但这可能导致企微收到大量中间消息，只显示最后一部分。我建议修改为类似Python的方式：每次刷新动态生成回复：

● Read(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\service\
      wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Read 80 lines

● 我需要简化并修正流式消息的处理逻辑。问题是：每次SSE消息都存储到队列，但企微可能不会正确处理这么多中间消息。

  让我修改为更符合企微预期的方式：

● Update(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Added 4 lines, removed 13 lines                                       
      588              // 获取该streamId的消息队列                          


      589              Queue<String> messageQueue =                      
           streamMessageStorage.get(streamId);                           
      590                                                                
      591 -            // 优先从队列中获取消息                              
            

      591 +            // 从队列中获取下一个消息（如果有的话）              
                    

      592              if (messageQueue != null &&                       
           !messageQueue.isEmpty()) {                                    
      593                  String nextMessage = messageQueue.poll();     
      594                  if (nextMessage != null) {                    
     ...
      601              // 队列为空时，检查是否已完成处理                    


      602              AtomicBoolean isCompleted =                       
           streamStatusMap.get(streamId);                                
      603              if (isCompleted != null && isCompleted.get()) {   
      604 -                //                                            
          -已完成，检查是否还有最后一次消息（完成消息）                     
                        

      605 -                // 注意：onCompletion中会将最终消息存储到队列    
                 

          -，但由于竞态条件可能还没被poll到                                 
                

      606 -                // 再次检查队列，确保获取到最终消息              
                  

      607 -                if (messageQueue != null &&                   
          -!messageQueue.isEmpty()) {                                    
      608 -                    String finalMessage =                     
          -messageQueue.poll();                                          
      609 -                    if (finalMessage != null) {               
      610 -                        log.debug("Retrieved final stream     
          -message for streamId: {}", streamId);                         
      611 -                        return finalMessage;                  
      612 -                    }                                         
      613 -                }                                             
      614 -                // 真正没有消息了，返回结束标记                  
                

      615 -                log.debug("Stream processing truly completed  
          -for streamId: {}", streamId);                                 
      604 +                // 已完成且队列为空，返回空内容的结束标记        
                     

      605 +                // 企微会显示已累积的内容                        
             

      606 +                log.debug("Stream processing completed for    
          +streamId: {}, returning empty finish message", streamId);     
      607                  return createStreamReply(streamId, "", true,  
           timestamp, nonce, config);                                    
      608              } else {                                          
      609                  // 还在处理中，返回等待消息                      



● 现在我意识到核心问题：每次SSE消息都被存储到队列，导致企微收到太多中间消息，最终可能只显示最后一个。

  根据企微的流式消息机制，每次刷新应该返回增量内容。但当前实现每次都返回累积内容，这可能导致企微不断覆盖显示。

  让我修改为只发送最终的完整回复，不发送中间消息：

● Update(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Added 8 lines, removed 12 lines                                       
      681                  if (messageContent != null &&                 
           !messageContent.isEmpty()) {                                  
      682                      completeContent.append(messageContent);   
      683                      int count =                               
           messageCount.incrementAndGet();                               
      684 -                                                              
      685 -                    // 创建流式消息                              
        

      686 -                    boolean isFinish = false; //              
          -实际的finish状态需要从业务逻辑判断                               
                

      687 -                    String streamMessage =                    
          -createStreamReply(streamId, messageContent, isFinish,         
      688 -                                                              
          -timestamp, nonce, config);                                    
      689 -                    capturedMessages.add(streamMessage);      
      690 -                                                              
      691 -                    // 存储消息供刷新请求使用                    
             

      692 -                    storeStreamMessage(streamId,              
          -streamMessage);                                               
      693 -                                                              
      694 -                    log.debug("Captured SSE message {}/{} for 
          - streamId: {}, content length: {}",                           
      684 +                                                              
      685 +                    // 注意：不在此处存储中间消息到队列          
                  

      686 +                    //                                        
          +企微的流式消息机制是：每次刷新返回增量内容，而不是累积内容       
                               

      687 +                    // 如果把每个SSE消息都存储到队列，企微会     
                   

          +收到太多中间消息，最终可能只显示最后一个                         
                      

      688 +                    // 我们只在 onCompletion                     
      

          +时存储最终的完整回复                                             
            

      689 +                                                              
      690 +                    log.debug("Captured SSE message {}/{} for 
          + streamId: {}, content length: {}",                           
      691                              count, "ongoing", streamId,       
           messageContent.length());                                     
      692                  }                                             
      693              } catch (Exception e) {                           
      694                  log.error("Error processing SSE message for   
           streamId: {}", streamId, e);                                  
      695              }                                                 
      696 -                                                              
      696 +                                                              
      697              // 调用父类方法确保正常的SSE处理                     


      698              super.send(object);                               
      699          }                                                     

● 现在让我确认 onCompletion 中的最终消息存储逻辑：

● Read(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\service\
      wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Read 60 lines

● 修复已完成。现在的逻辑是：

  修复后的流式消息流程

  1. 初始请求

  // 返回初始消息
  return createStreamReply(streamId, "🔍 正在为您处理请求...", false, ...);

  2. SSE 消息捕获（不存储中间消息）

  // 只累积内容到 completeContent，不存储到队列
  completeContent.append(messageContent);
  // 不调用 storeStreamMessage()

  3. 完成时存储最终完整回复

  // onCompletion 时，将完整的回复存储到队列
  String finalMessage = createStreamReply(streamId, completeContent.toString(), true, ...);
  storeStreamMessage(streamId, finalMessage);

  4. 刷新请求

  // 从队列中获取最终回复并返回
  if (messageQueue != null && !messageQueue.isEmpty()) {
      return messageQueue.poll();  // 返回大模型的完整回复
  }

  ---
  修复效果对比

  | 修复前                           | 修复后                           |
  |----------------------------------|----------------------------------|
  | 每个SSE消息都存储到队列          | 只存储最终的完整回复             |
  | 企微收到大量中间消息             | 企微只收到一次完整回复           |
  | 可能只显示最后一个消息           | 显示大模型的完整回复             |
  | finish=true 返回 "✅ 处理已完成" | finish=true 返回大模型的完整内容 |

● 现在 processStreamRefresh 会正确返回大模型的完整回复。修复后的流程：

  1. 初始请求 → 返回 "🔍 正在为您处理请求..." (finish=false)
  2. 中间刷新 → 返回 "⏳ 仍在处理中，请稍候..." (finish=false)
  3. 完成后刷新 → 返回大模型的完整回复 (finish=true)

  关键修改：
  - send() 方法不再将中间消息存储到队列
  - 只在 onCompletion 时将最终的完整回复存储到队列
  - processStreamRefresh 会从队列中获取并返回完整的回复

> 按我的理解：企微第一次发送的是text类型，然后我发送stream类型返回，之后企微
会发送stream类型，带有我之前返回的id，获取更多回复，这时的代码逻辑是有返回大
模型回复的内容吗？我看在处理stream类型的流程，最后返回的是空的 // 
队列为空时，检查是否已完成处理
            AtomicBoolean isCompleted = streamStatusMap.get(streamId);
            if (isCompleted != null && isCompleted.get()) {
                // 已完成且队列为空，返回空内容的结束标记
                // 企微会显示已累积的内容
                log.debug("Stream processing completed for streamId: {}, 
returning empty finish message", streamId);
                return createStreamReply(streamId, "", true, timestamp, 
nonce, config);
            } else {
                // 还在处理中，返回等待消息
                return createStreamReply(streamId, "⏳ 
仍在处理中，请稍候...", false, timestamp, nonce, config);
            }，而处理text类型的流程，它的 CapturingSseEmitter 
capturingEmitter = new CapturingSseEmitter(
                    360000L, // 6分钟超时
                    streamId,
                    timestamp,
                    nonce,
                    config,
                    streamMessages,
                    completeReply,
                    messageCounter
                );有在处理后面stream的请求吗？ 

● 你的理解是正确的！我来解释一下当前的流程和问题：

  当前流程

  1. text 类型请求（首次）

  企微发送: { "msgtype": "text", ... }
          ↓
  processTextMessageSync() 被调用
          ↓
  - 生成 streamId
  - 初始化状态为 false
  - 启动异步任务：创建 CapturingSseEmitter
  - 返回初始消息: { "stream": { "id": "abc123", "finish": false, "content": "🔍 正在..." } }

  2. stream 类型刷新请求（后续）

  企微发送: { "msgtype": "stream", "stream": { "id": "abc123" } }
          ↓
  processStreamRefresh() 被调用
          ↓
  从队列获取消息
          ↓
  如果队列非空: 返回队列中的消息
  如果队列为空且已完成: 返回空内容 ⚠️ 问题在这里！

  ---
  问题分析

  问题1：队列为空时返回空内容

  // 队列为空时，检查是否已完成处理
  AtomicBoolean isCompleted = streamStatusMap.get(streamId);
  if (isCompleted != null && isCompleted.get()) {
      // 返回空内容的结束标记
      return createStreamReply(streamId, "", true, timestamp, nonce, config);  // ⚠️ 空内容！
  }

  这导致企微收到空的回复！

  问题2：中间消息未存储

  之前我修改了 send() 方法，不再存储中间消息到队列：
  // 注意：不在此处存储中间消息到队列
  // 我们只在 onCompletion 时存储最终的完整回复

  但这导致队列中只有一条最终消息，如果企微刷新时这条消息已经被 poll 走了，后续刷新就只能收到空内容。

  ---
  修复方案

  需要每次刷新都返回完整内容，即使队列为空。让我修改：

● Read(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\service\
      wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Read 50 lines

● 我需要修改 processStreamRefresh 的逻辑。关键是：当队列为空且已完成时，不应该返回空内容，而应该返回之前存储的最终消息。

  但当前问题是：如果队列中的最终消息被 poll 走了，后续刷新就收不到内容了。

  最佳方案是：让最终消息一直留在队列中（不 poll），企微每次刷新都能获取到它。

● Update(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Added 22 lines, removed 13 lines                                      
      588              // 获取该streamId的消息队列                          


      589              Queue<String> messageQueue =                      
           streamMessageStorage.get(streamId);                           
      590                                                                
      591 -            // 从队列中获取下一个消息（如果有的话）              
                    

      591 +            // 优先检查是否已完成处理                            
             

      592 +            AtomicBoolean isCompleted =                       
          +streamStatusMap.get(streamId);                                
      593 +            if (isCompleted != null && isCompleted.get()) {   
      594 +                // 已完成处理                                    
       

      595 +                if (messageQueue != null &&                   
          +!messageQueue.isEmpty()) {                                    
      596 +                    // 队列中有最终消息，使用 peek 不            
              

          +poll（不移除）                                                   
       

      597 +                    String finalMessage =                     
          +messageQueue.peek();                                          
      598 +                    if (finalMessage != null) {               
      599 +                        log.debug("Retrieved final stream     
          +message for streamId: {} (not removing from queue)",          
      600 +                                 streamId);                   
      601 +                        return finalMessage;                  
      602 +                    }                                         
      603 +                }                                             
      604 +                //                                            
          +队列为空但已完成，说明最终消息可能已被清理或丢失                 
                          

      605 +                log.warn("Stream completed but queue is empty 
          + for streamId: {}", streamId);                                
      606 +                return createStreamReply(streamId, "✅         
          +处理已完成", true, timestamp, nonce, config);                    
       

      607 +            }                                                 
      608 +                                                              
      609 +            // 还在处理中，检查队列中是否有消息                  
                  

      610              if (messageQueue != null &&                       
           !messageQueue.isEmpty()) {                                    
      611                  String nextMessage = messageQueue.poll();     
      612                  if (nextMessage != null) {                    
      613 -                    log.debug("Retrieved                      
          -stream message for streamId: {}, remaining queue size: {}",   
      613 +                    log.debug("Retrieved intermediate         
          +stream message for streamId: {}, remaining queue size: {}",   
      614                               streamId, messageQueue.size());  
      615                      return nextMessage;                       
      616                  }                                             
      617              }                                                 
      618                                                                
      619 -            // 队列为空时，检查是否已完成处理                    
                 

      620 -            AtomicBoolean isCompleted =                       
          -streamStatusMap.get(streamId);                                
      621 -            if (isCompleted != null && isCompleted.get()) {   
      622 -                // 已完成且队列为空，返回空内容的结束标记        
                     

      623 -                // 企微会显示已累积的内容                        
             

      624 -                log.debug("Stream processing completed for    
          -streamId: {}, returning empty finish message", streamId);     
      625 -                return createStreamReply(streamId, "", true,  
          -timestamp, nonce, config);                                    
      626 -            } else {                                          
      627 -                // 还在处理中，返回等待消息                      
              

      628 -                return createStreamReply(streamId, "⏳         
          -仍在处理中，请稍候...", false, timestamp, nonce, config);        
           

      629 -            }                                                 
      619 +            // 队列为空且还在处理中，返回等待消息                
                   

      620 +            return createStreamReply(streamId, "⏳             
          +仍在处理中，请稍候...", false, timestamp, nonce, config);        
           

      621                                                                
      622          } catch (Exception e) {                               
      623              log.error("Failed to process stream refresh for   
           streamId: {}", streamId, e);                                  

● 现在修改完成。让我总结一下修复后的流程：

  修复后的流式消息流程

  1. text 类型请求（首次）

  企微发送: { "msgtype": "text", "text": { "content": "你好" } }
          ↓
  processTextMessageSync() 被调用
          ↓
  - 生成 streamId = "abc123"
  - 初始化状态为 false
  - 启动异步任务：创建 CapturingSseEmitter
  - 返回初始消息: { "stream": { "id": "abc123", "finish": false, "content": "🔍 正在..." } }

  2. SSE 消息捕获（异步进行）

  botChatService.chatMessageBot() 返回 SSE 流
          ↓
  CapturingSseEmitter.send() 被多次调用
          ↓
  每次调用时：
  - 捕获消息内容：completeContent.append(messageContent)
  - 不存储中间消息到队列
          ↓
  完成后 onCompletion() 被调用：
  - 创建最终消息（包含大模型的完整回复）
  - storeStreamMessage(streamId, 最终消息)
  - 设置 isCompleted = true

  3. stream 类型刷新请求（后续）

  企微发送: { "msgtype": "stream", "stream": { "id": "abc123" } }
          ↓
  processStreamRefresh() 被调用
          ↓
  检查 isCompleted:
    - 如果已完成:
      - 使用 peek() 获取最终消息（不从队列移除）
      - 返回大模型的完整回复 ✅
    - 如果未完成:
      - 如果队列有消息: poll() 返回中间消息
      - 如果队列为空: 返回 "⏳ 仍在处理中..."

  ---
  关键修改点

  | 修改位置                     | 修改内容                  | 原因                                             |
  |------------------------------|---------------------------|--------------------------------------------------|
  | processStreamRefresh:597     | 使用 peek() 而不是 poll() | 让最终消息一直留在队列中，企微每次刷新都能获取到 |
  | processStreamRefresh:593-607 | 优先检查 isCompleted      | 确保完成后返回完整回复而不是空内容               |
  | CapturingSseEmitter.send     | 不存储中间消息            | 防止企微收到太多中间消息，只关注最终完整回复     |

  ---
  关于 CapturingSseEmitter 的说明

  CapturingSseEmitter 是内部类，它的作用是：
  1. 捕获 botChatService.chatMessageBot() 返回的 SSE 流
  2. 累积所有消息内容到 completeContent
  3. 在完成时将最终完整回复存储到队列

  它不会直接处理 stream 类型的刷新请求。刷新请求是由 processStreamRefresh() 方法处理的，它会从队列中获取消息返回给企微。

  现在企微每次刷新时，都能获取到大模型的完整回复（而不是空内容）。

> @console\backend\hub\src\main\java\com\iflytek\astron\console\hub\service\
wechat\impl\WechatRobotMessageServiceImpl.java 
的processStreamRefresh方法，return finalMessage;和 return 
nextMessage;是否不符合企微的规范，正确的是应该return 
createStreamReply(streamId, "✅ 处理已完成", true, timestamp, nonce, 
config); 

● 你说得对！问题在于 return finalMessage; 和 return nextMessage;。

  问题分析

  // 第597行：直接返回队列中存储的加密消息
  String finalMessage = messageQueue.peek();
  if (finalMessage != null) {
      return finalMessage;  // ⚠️ 问题：这是之前用旧的 timestamp/nonce 加密的！
  }

  问题：队列中存储的消息是用旧的 timestamp 和 nonce 加密的，但每次企微刷新请求会带来新的 timestamp 和 nonce。

  企微规范要求：每次刷新都应该用新的 timestamp 和 nonce 重新创建并加密响应。

  修复方案

● Update(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Added 13 lines, removed 8 lines                                       
      577       */                                                       
      578      public String processStreamRefresh(String streamId,       
           String timestamp, String nonce, WechatBotConfig config) {     
      579          try {                                                 
      580 -            log.debug("Processing stream refresh for streamId:
          - {}", streamId);                                              
      580 +            log.debug("Processing stream refresh for streamId:
          + {}, timestamp={}, nonce={}", streamId, timestamp, nonce);    
      581                                                                
      582              // 检查 streamId 是否存在                            


      583              if (!streamStatusMap.containsKey(streamId)) {     
     ...
      593              if (isCompleted != null && isCompleted.get()) {   
      594                  // 已完成处理                                    


      595                  if (messageQueue != null &&                   
           !messageQueue.isEmpty()) {                                    
      596 -                    // 队列中有最终消息，使用 peek 不 
     poll（不移除）           
      596 +                    // 队列中有最终消息，使用 peek 
     获取（不移除）               
      597                      String finalMessage =                     
           messageQueue.peek();                                          
      598                      if (finalMessage != null) {               
      599 -                        log.debug("Retrieved final stream     
          -message for streamId: {} (not removing from queue)",          
      600 -                                 streamId);                   
      601 -                        return finalMessage;                  
      599 +                        log.debug("Stream completed for       
          +streamId: {}, returning completion message", streamId);       
      600 +                        // 重要：必须使用当前请求的              
              

          +timestamp/nonce 重新创建响应                                     
        

      601 +                        //                                    
          +不能直接返回队列中用旧参数加密的消息                             
                    

      602 +                        return createStreamReply(streamId,    
          +"✅ 处理已完成", true, timestamp, nonce, config);                
        

      603                      }                                         
      604                  }                                             
      605 -                // 
     队列为空但已完成，说明最终消息可能已被清理或丢失                   
      606 -                log.warn("Stream completed but queue is empty 
          - for streamId: {}", streamId);                                
      605 +                // 队列为空但已完成，返回完成消息                
                 

      606 +                log.debug("Stream completed (queue empty)     
          + for streamId: {}", streamId);                                
      607                  return createStreamReply(streamId, "✅         
           处理已完成", true, timestamp, nonce, config);                    


      608              }                                                 
      609                                                                
     ...
      612                  if (nextMessage != null) {                    
      613                      log.debug("Retrieved intermediate stream  
           message for streamId: {}, remaining queue size: {}",          
      614                               streamId, messageQueue.size());  
      615 -                    return nextMessage;                       
      615 +                    // 注意：这里直接返回队列中的消息是错误的    
                     

      616 +                    // 应该用当前请求的 timestamp/nonce          
          

          +重新创建                                                         
      

      617 +                    //                                        
          +但由于队列中存储的是未解密的消息，我们无法重新加密               
                           

      618 +                    // 所以这里应该返回处理中消息                
               

      619 +                    return createStreamReply(streamId, "⏳     
          +仍在处理中，请稍候...", false, timestamp, nonce, config);        
           

      620                  }                                             
      621              }                                                 
      622                                                                

● 已修复。现在每次刷新都会使用当前请求的 timestamp 和 nonce 重新创建响应，符合企微规范。

  修复后的流程

  完成状态

  if (isCompleted != null && isCompleted.get()) {
      // 不管队列中是否有消息，都用当前请求的 timestamp/nonce 创建响应
      return createStreamReply(streamId, "✅ 处理已完成", true, timestamp, nonce, config);
  }

  处理中状态

  // 不管队列中是否有消息，都用当前请求的 timestamp/nonce 创建响应
  return createStreamReply(streamId, "⏳ 仍在处理中，请稍候...", false, timestamp, nonce, config);

  ---
  关键修改

  | 修改前                     | 修改后                                                                               |
  |----------------------------|--------------------------------------------------------------------------------------|
  | return finalMessage;       | return createStreamReply(streamId, "✅ 处理已完成", true, timestamp, nonce, config); |
  | 使用队列中旧参数加密的消息 | 使用当前请求的新 timestamp/nonce 重新创建并加密                                      |

  这样每次刷新响应都是用新的 timestamp 和 nonce 加密的，符合企微的加密签名验证要求。

> 不是，你返回的是内容都是✅ 
处理已完成？不是应该返回队列里的消息内容吗？这样用户才能看到大模型的回复 

● 你说得完全正确！我理解错了。让我重新分析问题：

  问题根源

  队列中存储的是已经加密的消息，用的是旧的 timestamp 和 nonce。但每次企微刷新请求会带来新的 timestamp 和 nonce，需要用新参数重新加密。

  正确的解决方案

  需要存储原始的未加密内容，每次刷新时用新的 timestamp 和 nonce 重新加密。

● Read(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\service\
      wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Read 25 lines

● 需要添加一个 Map 来存储原始未加密的内容，每次刷新时用新的 timestamp 和 nonce 重新加密：

● Update(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Added 5 lines, removed 2 lines                                        
      47      private final ChatBotDataService chatBotDataService;       
      48      private final BotChatService botChatService;               
      49      private final StreamMessageUtil streamMessageUtil;         
      50 -                                                               
      51 -    // 流式消息存储映射：streamId -> 消息队列                     
               

      50 +                                                               
      51 +    // 流式消息存储映射：streamId -> 原始未加密的内容队列         
                     

      52 +    // 注意：不存储加密后的消息，因为每次刷新需要用新的timesta    
                          

         +mp/nonce重新加密                                                  
      

      53      private final Map<String, Queue<String>>                   
          streamMessageStorage = new ConcurrentHashMap<>();              
      54      // 流式处理状态映射：streamId -> 是否已完成                   


      55      private final Map<String, AtomicBoolean> streamStatusMap = 
           new ConcurrentHashMap<>();                                    
      56 +    // 流式消息的完整回复内容映射：streamId -> 最终的完整内容     
                       

      57 +    private final Map<String, String> streamCompleteContent =  
         +new ConcurrentHashMap<>();                                     
      58                                                                 
      59      /**                                                        
      60       * 定时清理过期的流式消息数据                                 



● 现在修改 onCompletion 来存储最终的完整原始内容：

● Read(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\service\
      wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Read 50 lines

● Update(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Added 4 lines, removed 6 lines                                        
      655                                                                
      656              // 设置完成监听器                                    


      657              this.onCompletion(() -> {                         
      658 -                log.debug("CapturingSseEmitter completed for  
          -streamId: {}", streamId);                                     
      659 -                // 发送最终完成消息并存储                        
             

      660 -                String finalMessage = createStreamReply       
          -(this.streamId, this.completeContent.toString(), true,        
      661 -                                                              
          -this.timestamp, this.nonce, this.config);                     
      662 -                this.capturedMessages.add(finalMessage);      
      663 -                storeStreamMessage(this.streamId,             
          -finalMessage);                                                
      658 +                log.debug("CapturingSseEmitter completed for  
          +streamId: {}, content length: {}", streamId                   
          +, this.completeContent.length());                             
      659 +                //                                            
          +存储原始的完整内容（未加密），供刷新请求时使用                   
                         

      660 +                streamCompleteContent.put                     
          +(this.streamId, this.completeContent.toString());             
      661 +                log.debug("Stored complete content for        
          +streamId: {}, length: {}", streamId,                          
          +this.completeContent.length());                               
      662                  // 标记处理完成 -                                


           使用已存在的状态，不重新创建                                     


      663                  AtomicBoolean status =                        
           streamStatusMap.get(this.streamId);                           
      664                  if (status != null) {                         

● 现在修改 processStreamRefresh 方法，从 streamCompleteContent 获取原始内容并用新的 timestamp 和 nonce 加密：

● Update(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Added 13 lines, removed 32 lines                                      
      588                  return createStreamReply(streamId, "❌         
           请求已过期或不存在", true, timestamp, nonce, config);            


      589              }                                                 
      590                                                                
      591 -            // 获取该streamId的消息队列                          
          

      592 -            Queue<String> messageQueue =                      
          -streamMessageStorage.get(streamId);                           
      593 -                                                              
      594 -            // 优先检查是否已完成处理                            
             

      591 +            // 检查是否已完成处理                                
           

      592              AtomicBoolean isCompleted =                       
           streamStatusMap.get(streamId);                                
      593              if (isCompleted != null && isCompleted.get()) {   
      594 -                // 已完成处理                                    
       

      595 -                if (messageQueue != null &&                   
          -!messageQueue.isEmpty()) {                                    
      596 -                    // 队列中有最终消息，使用 peek               
             

          -获取（不移除）                                                   
         

      597 -                    String finalMessage =                     
          -messageQueue.peek();                                          
      598 -                    if (finalMessage != null) {               
      599 -                        log.debug("Stream completed for       
          -streamId: {}, returning completion message", streamId);       
      600 -                        // 重要：必须使用当前请求的              
              

          -timestamp/nonce 重新创建响应                                     
        

      601 -                        //                                    
          -不能直接返回队列中用旧参数加密的消息                             
                    

      602 -                        return createStreamReply(streamId,    
          -"✅ 处理已完成", true, timestamp, nonce, config);                
        

      603 -                    }                                         
      594 +                // 已完成处理，从 streamCompleteContent          
         

          +获取原始的完整内容                                               
           

      595 +                String completeContent =                      
          +streamCompleteContent.get(streamId);                          
      596 +                if (completeContent != null &&                
          +!completeContent.isEmpty()) {                                 
      597 +                    log.debug("Stream completed for streamId: 
          + {}, returning complete content (length: {})",                
      598 +                             streamId,                        
          +completeContent.length());                                    
      599 +                    // 用当前请求的 timestamp/nonce              
        

          +重新加密并返回大模型的完整回复                                   
                 

      600 +                    return createStreamReply(streamId,        
          +completeContent, true, timestamp, nonce, config);             
      601 +                } else {                                      
      602 +                    // 没有完整内容                              
        

      603 +                    log.warn("Stream completed but no content 
          + found for streamId: {}", streamId);                          
      604 +                    return createStreamReply(streamId, "✅     
          +处理已完成", true, timestamp, nonce, config);                    
       

      605                  }                                             
      606 -                // 队列为空但已完成，返回完成消息                
                 

      607 -                log.debug("Stream completed (queue empty) for 
          - streamId: {}", streamId);                                    
      608 -                return createStreamReply(streamId, "✅         
          -处理已完成", true, timestamp, nonce, config);                    
       

      606              }                                                 
      607                                                                
      608 -            // 还在处理中，检查队列中是否有消息                  
                  

      609 -            if (messageQueue != null &&                       
          -!messageQueue.isEmpty()) {                                    
      610 -                String nextMessage = messageQueue.poll();     
      611 -                if (nextMessage != null) {                    
      612 -                    log.debug("Retrieved intermediate stream  
          -message for streamId: {}, remaining queue size: {}",          
      613 -                             streamId, messageQueue.size());  
      614 -                    // 注意：这里直接返回队列中的消息是错误的    
                     

      615 -                    // 应该用当前请求的 timestamp/nonce          
          

          -重新创建                                                         
      

      616 -                    //                                        
          -但由于队列中存储的是未解密的消息，我们无法重新加密               
                           

      617 -                    // 所以这里应该返回处理中消息                
               

      618 -                    return createStreamReply(streamId, "⏳     
          -仍在处理中，请稍候...", false, timestamp, nonce, config);        
           

      619 -                }                                             
      620 -            }                                                 
      621 -                                                              
      622 -            // 队列为空且还在处理中，返回等待消息                
                   

      608 +            // 还在处理中，返回等待消息                          
              

      609              return createStreamReply(streamId, "⏳             
           仍在处理中，请稍候...", false, timestamp, nonce, config);        


      610                                                                
      611          } catch (Exception e) {                               

● 现在需要更新清理方法，同时清理 streamCompleteContent：

● Update(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Added 11 lines, removed 6 lines                                       
      421       */                                                       
      422      private void cleanupExpiredStreams() {                    
      423          try {                                                 
      424 -            // 清理已完成且超过1小时的数据                       
               

      424 +            // 清理已完成的数据                                  
          

      425              streamStatusMap.entrySet().removeIf(entry -> {    
      426                  String streamId = entry.getKey();             
      427                  AtomicBoolean isCompleted = entry.getValue(); 
      428 -                                                              
      428 +                                                              
      429                  if (isCompleted.get()) {                      
      430 -                    // 移除已完成的消息队列                      
            

      430 +                    // 移除已完成的数据                          
          

      431                      streamMessageStorage.remove(streamId);    
      432 +                    streamCompleteContent.remove(streamId);   
      433                      log.debug("Cleaned up completed stream:   
           {}", streamId);                                               
      434                      return true;                              
      435                  }                                             
      436                  return false;                                 
      437              });                                               
      438 -                                                              
      439 -            // 清理没有任何状态记录的孤立消息队列                
                   

      438 +                                                              
      439 +            // 清理没有任何状态记录的孤立数据                    
                 

      440              streamMessageStorage.entrySet().removeIf(entry -> 
            {                                                            
      441                  String streamId = entry.getKey();             
      442                  return                                        
           !streamStatusMap.containsKey(streamId);                       
      443              });                                               
      444 -                                                              
      444 +            streamCompleteContent.entrySet().removeIf(entry   
          +-> {                                                          
      445 +                String streamId = entry.getKey();             
      446 +                return                                        
          +!streamStatusMap.containsKey(streamId);                       
      447 +            });                                               
      448 +                                                              
      449          } catch (Exception e) {                               
      450              log.warn("Failed to cleanup expired streams", e); 
      451          }                                                     

● Update(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Added 1 line                                                        
      573          try {                                                 
      574              streamMessageStorage.remove(streamId);            
      575              streamStatusMap.remove(streamId);                 
      576 +            streamCompleteContent.remove(streamId);           
      577              log.info("Manually cleaned up stream data for     
           streamId: {}", streamId);                                     
      578          } catch (Exception e) {                               
      579              log.error("Failed to manually cleanup stream data 
            for streamId: {}", streamId, e);                             

● 修复完成。现在的逻辑是：

  修复后的流程

  1. text 类型请求（首次）

  企微发送: { "msgtype": "text", ... }
          ↓
  processTextMessageSync()
          ↓
  - 生成 streamId
  - 初始化状态为 false
  - 启动异步任务：创建 CapturingSseEmitter
  - 返回初始消息: { "stream": { "id": "abc123", "finish": false, "content": "🔍 正在..." } }

  2. SSE 消息捕获（异步）

  CapturingSseEmitter.send() 被多次调用
          ↓
  completeContent.append(messageContent)  // 累积大模型的回复
          ↓
  完成后 onCompletion():
  streamCompleteContent.put(streamId, completeContent.toString())  // 存储原始内容
  streamStatusMap.get(streamId).set(true)  // 标记完成

  3. stream 类型刷新请求（后续）

  企微发送: { "msgtype": "stream", "stream": { "id": "abc123" } }
          ↓
  processStreamRefresh()
          ↓
  检查 isCompleted:
    - 如果已完成:
      completeContent = streamCompleteContent.get(streamId)  // 获取原始内容
      return createStreamReply(streamId, completeContent, true, timestamp, nonce, config)
      // ✅ 用当前请求的 timestamp/nonce 重新加密，返回大模型的完整回复！
    - 如果未完成:
      return createStreamReply(streamId, "⏳ 仍在处理中...", false, timestamp, nonce, config)

  ---
  关键修改点

  | 位置             | 修改内容                                                                          |
  |------------------|-----------------------------------------------------------------------------------|
  | 第57行           | 新增 streamCompleteContent Map 存储原始内容                                       |
  | 第660行          | onCompletion 存储原始内容到 streamCompleteContent                                 |
  | 第595-600行      | processStreamRefresh 从 streamCompleteContent 获取内容并用新 timestamp/nonce 加密 |
  | 第432、444-447行 | 清理方法同时清理 streamCompleteContent                                            |
  | 第576行          | cleanupStreamData 同时清理 streamCompleteContent                                  |

  现在企微每次刷新都能收到大模型的完整回复（用新的 timestamp 和 nonce 加密），符合企微的签名验证要求。

> 队列中存储的是已经加密的消息吗？ 

● 是的！让我检查当前代码的状态：

● Read(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\service\
      wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Read 80 lines

● 让我检查当前代码中 streamMessageStorage 的使用情况：

● Search(pattern: "streamMessageStorage", path:
        "console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java", output_mode:
        "content")
  ⎿  Found 8 lines (ctrl+o to expand)

● 让我检查 storeStreamMessage 方法是否还在被调用：

● Read(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\service\
      wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Read 30 lines

● 让我检查 onCompletion 当前是否还在调用 storeStreamMessage：

● Read(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\service\
      wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Read 40 lines

● 看到问题了！当前代码存在不一致：

  当前状态分析

  | 位置                           | 存储的内容                     | 是否加密            |
  |--------------------------------|--------------------------------|---------------------|
  | onCompletion (第660行)         | streamCompleteContent.put()    | ❌ 否，原始内容     |
  | onError (第665行)              | storeStreamMessage()           | ⚠️ 是，加密后的消息 |
  | processStreamRefresh (第595行) | 从 streamCompleteContent.get() | 用新参数重新加密    |

  问题：onError 中还在用 storeStreamMessage() 存储加密后的消息，但 processStreamRefresh 只从 streamCompleteContent 获取内容！

  需要修复 onError 中的逻辑：

● Update(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Added 4 lines, removed 3 lines                                        
      660              // 设置错误监听器                                    


      661              this.onError(throwable -> {                       
      662                  log.error("CapturingSseEmitter error for      
           streamId: {}", streamId, throwable);                          
      663 -                String errorMessage =                         
          -createStreamReply(this.streamId, "处理过程中出现错误", true,     
           

      664 -                                                              
          -this.timestamp, this.nonce, this.config);                     
      665 -                storeStreamMessage                            
          -(this.streamId, errorMessage);                                
      663 +                // 存储错误信息到                                
         

          +streamCompleteContent，确保与 onCompletion 一致                  
        

      664 +                String errorMessage = "处理过程中出现错误："     
            

          ++ throwable.getMessage();                                     
      665 +                streamCompleteContent.put                     
          +(this.streamId, errorMessage);                                
      666 +                log.debug("Stored error content for streamId: 
          + {}", streamId);                                              
      667                  // 标记处理完成（出错也算完成）-                 


           使用已存在的状态，不重新创建                                     


      668                  AtomicBoolean status =                        
           streamStatusMap.get(this.streamId);                           
      669                  if (status != null) {                         

● 现在更新 streamMessageStorage 的注释，因为现在它不再用于存储加密后的消息：

● Update(console\backend\hub\src\main\java\com\iflytek\astron\console\hub\servic
        e\wechat\impl\WechatRobotMessageServiceImpl.java)
  ⎿  Added 4 lines, removed 3 lines                                        
      48      private final BotChatService botChatService;               
      49      private final StreamMessageUtil streamMessageUtil;         
      50                                                                 
      51 -    // 流式消息存储映射：streamId -> 原始未加密的内容队列         
                     

      52 -    // 注意：不存储加密后的消息，因为每次刷新需要用新的timesta    
                          

         -mp/nonce重新加密                                                  
      

      51 +    // 流式消息存储映射：streamId -> 
     消息队列（当前未使用，保留用于扩展）                 
      52 +    // 注意：原始未加密的内容已改为存储在                         
                   

         +streamCompleteContent 中                                        
      53 +    // streamMessageStorage 已不再用于存储加密后的消息            
               

      54      private final Map<String, Queue<String>>                   
          streamMessageStorage = new ConcurrentHashMap<>();              
      55      // 流式处理状态映射：streamId -> 是否已完成                   


      56      private final Map<String, AtomicBoolean> streamStatusMap = 
           new ConcurrentHashMap<>();                                    
      57 -    // 流式消息的完整回复内容映射：streamId -> 最终的完整内容     
                       

      57 +    // 流式消息的完整回复内容映射：streamId -> 
     最终的完整原始内容（未加密）                
      58      private final Map<String, String> streamCompleteContent =  
          new ConcurrentHashMap<>();                                     
      59                                                                 
      60      /**                                                        

● 修复完成。现在回答你的问题：

  当前队列存储状态

  streamMessageStorage

  - 当前不再使用，已注释说明保留用于扩展
  - 之前存储的是加密后的消息

  streamCompleteContent（新增）

  - 存储的是原始未加密的内容
  - onCompletion: streamCompleteContent.put(streamId, completeContent.toString())
  - onError: streamCompleteContent.put(streamId, errorMessage)

  processStreamRefresh

  String completeContent = streamCompleteContent.get(streamId);  // 获取原始未加密内容
  return createStreamReply(streamId, completeContent, true, timestamp, nonce, config);
  // ✅ 用当前请求的 timestamp/nonce 重新加密

  ---
  总结

  | 存储位置                  | 存储内容       | 是否加密          |
  |---------------------------|----------------|-------------------|
  | streamMessageStorage      | 未使用（保留） | -                 |
  | streamCompleteContent     | 原始内容       | ❌ 否             |
  | processStreamRefresh 返回 | 重新加密的消息 | ✅ 是（用新参数） |

  现在企微每次刷新都能收到用新的 timestamp 和 nonce 加密的大模型完整回复。



